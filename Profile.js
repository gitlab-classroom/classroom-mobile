'use strict';

var React = require('react-native');
var {
  PixelRatio,
  AppRegistry,
  NavigatorIOS,
  Image,
  Platform,
  StyleSheet,
  Text,
  TouchableHighlight,
  TouchableNativeFeedback,
  View,

  } = React;

var Profile = React.createClass({
  render: function() {
    return (
      <View>
      </View>
    );
  }
});
